
// objetos

const persona = {
  nombre: "Rafael",
  edad: 28,
  ciudad: "Bogotá"
};

console.log(persona['nombre']); // Rafael
console.log(persona.edad); // 28

// arrays

const numeros = [10, 20, 30, 40];
console.log(numeros[3]); // 10 (posición 0)
console.log(numeros.length); // 4 (tamaño del array)
